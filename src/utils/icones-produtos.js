import { Candy, Gift, Egg, Gem, Drumstick, CakeSlice, PartyPopper, Cookie } from 'lucide-react';

export const IconesPorNome = {
  Candy,
  Gift,
  Egg,
  Gem,
  Drumstick,
  CakeSlice,
  PartyPopper,
  Cookie,
};

// fall-back para produtos sem icone
export const IconePadrao = Cookie;